---
title: тест — em dash в frontmatter не считается
---

Чистый абзац без нарушений, обычная короткая проза. Ровно два предложения тут.

```python
x = 1  # не просто код, а код с em dash — и стрелкой ->
```

| колонка | знаки > < = → |
|---|---|
| не только в таблице | но и можно — |

Строка с инлайн-кодом `a -> b — не просто так` тоже чистая.
